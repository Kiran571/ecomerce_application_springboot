server:
  port: 9090

spring:
  datasource:
    password: Kiran571
    url: jdbc:mysql://localhost:3306/jwt
    username: root
  jpa:
    hibernate:
      ddl-auto: create


  main:
    allow-circular-references: true

